{
  "name": "smartlog-clinic-backend",
  "description": "Mock SmartLog Clinic to receive data from CareSens SmartLog App",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "start-dev": "ts-node src/index.ts",
    "serve-docker": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@types/koa-router": "^7.4.4",
    "koa": "^2.14.2",
    "koa-body": "^6.0.1",
    "koa-router": "^12.0.0"
  }
}
